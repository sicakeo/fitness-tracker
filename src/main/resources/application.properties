spring.application.name=fitnesstracker
# The URL of your database (replace 'testdatabase' if yours is named differently)
spring.datasource.url=jdbc:mysql://localhost:3306/testdatabase?useSSL=false&serverTimezone=UTC

# Your MySQL credentials
spring.datasource.username=root
spring.datasource.password=BigBoySLooking18@

# The driver class
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect